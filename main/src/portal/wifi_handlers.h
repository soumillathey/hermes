/**
 * wifi_handlers.h
 * Internal header — declares handleSave() so wifi_ap.cpp
 * can register it as a WebServer route handler.
 */

#ifndef WIFI_HANDLERS_H
#define WIFI_HANDLERS_H

void handleSave();

#endif // WIFI_HANDLERS_H
